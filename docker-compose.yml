services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DJANGO_DEBUG=False
      - DJANGO_SECRET_KEY=change-this-to-a-long-random-string
    volumes:
      - sqlite_data:/app/db.sqlite3

volumes:
  sqlite_data:
